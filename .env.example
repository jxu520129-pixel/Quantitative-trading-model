# 运行安全：即使设置 LIVE，仍必须设置 ENABLE_LIVE_TRADING=true 并实现实盘适配器。
TRADING_MODE=PAPER
ENABLE_LIVE_TRADING=false
DATABASE_PATH=data/ashare_quant.db
QUANT_CONFIG=config/default.yaml

# 写入前请轮换任何曾粘贴到聊天或日志中的令牌。
TUSHARE_TOKEN=

# 可选：Tushare 第三方代理地址（付费转发服务）。留空=官方 api.tushare.pro；
# 使用代理时 TUSHARE_TOKEN 需填代理方下发的 Token。代理到期后接口报错，系统自动退回 AkShare 主源。
TUSHARE_API_URL=

# 此项留空时，Streamlit 操作控件将保持禁用状态。
DASHBOARD_PASSWORD=请在本机修改为强密码

# 可选：企业微信机器人 Webhook。
WECOM_WEBHOOK=

# 可选：使用 SSL 的 SMTP 邮件。
SMTP_HOST=
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_TO=

# 仅供后续实盘适配器使用；V1 模拟盘请保持为空。
QMT_ACCOUNT_ID=
QMT_USERDATA_PATH=
PTRADE_ACCOUNT_ID=
