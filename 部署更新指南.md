# 部署与更新指南

量化系统「本地开发 → 云端上线」的操作速查。

## 一、环境概览

| 环境 | 用途 | 关键信息 |
|---|---|---|
| 本地（Windows） | 改代码、跑测试、git 提交 | 代码目录 `C:\Users\Administrator\Desktop\Quantitative trading model`；本地 Docker 仅测试用，可随时关闭 |

| 云端（腾讯云） | 生产运行 | 广州 `106.55.12.64`，用户 `ubuntu`，代码在 `~/ashare-quant`，两个容器 scheduler + dashboard |

> 本地和云端完全独立，改代码、关本地 Docker 都不影响云端。

## 二、改功能 → 上线流程

```powershell
# ① 本地改代码（不用开 Docker）
# ② 提交推送（记得开 Clash 代理，否则连不上 GitHub）
cd "C:\Users\Administrator\Desktop\Quantitative trading model"
git add -A
git commit -m "改动说明"
git push
```

```bash
# ③ 云端拉取 + 重建（数据卷保留，不丢数据）
cd ~/ashare-quant
git pull
sudo docker compose up -d --build
sudo docker compose ps          # 确认两个容器 Up 且无 Restarting
```

## 三、常用命令速查

### 本地
```powershell
git status                    # 看改了哪些文件
git log --oneline -5          # 看最近提交
git push                      # 推送（开 Clash）
```

### 云端
```bash
cd ~/ashare-quant
sudo docker compose ps                        # 容器状态
sudo docker compose logs -f scheduler         # 看调度日志
sudo docker compose exec -T scheduler python -m ashare_quant.cli status   # 看账户
nano ~/ashare-quant/.env                      # 编辑云端的 .env（改完需 docker compose up -d --build 重建生效）
```

## 四、首次在新服务器部署

```bash
# 上传部署包（或 git clone，私有仓库需 token、大陆需代理）
cd ~ && unzip ashare_quant_deploy.zip -d ashare-quant
cd ashare-quant
sed -i 's|^DASHBOARD_PASSWORD=.*|DASHBOARD_PASSWORD=你的密码|' .env
bash deploy/cloud_setup.sh
```

## 五、注意事项（踩过的坑）

1. **代理**：本机推 GitHub 必须开着 Clash（git 已配 `http.proxy=127.0.0.1:7890`）；不用代理时 `git push` 会失败。
2. **数据卷**：`docker compose up -d --build` 重建不丢数据；**别用** `docker compose down -v`（会删数据卷）。
3. **config/default.yaml 覆盖**：云端把重训采样改成了 120（2G 内存），`git pull` 会把 config 覆盖回 300，需要时重新调或用 `.env` 的 `QUANT_CONFIG` 挂独立配置。
4. **`.env` 不进 git**：密码、SMTP、Tushare token 等都在服务器本地 `.env`，pull 不会动它。
5. **防火墙**：看板端口 8501 需在腾讯云控制台「防火墙」放行。
6. **数据库结构变更**：新增表/字段由 `database.py` 启动时自动迁移，重启容器即生效。
7. **GitHub 网络问题（大陆服务器）**：云端 `git pull` 若报 `GnuTLS recv error (-110)` 或连接超时，是 **github.com 443 端口被墙**（先 `curl -sS -o /dev/null -w "%{http_code}\n" --connect-timeout 8 https://github.com` 验证，返回 `000` 即不通），不是代码问题。解法：用镜像拉取。
   ```bash
   # ① 测哪个镜像通（返回 200/301/302 即通）
   for m in ghfast.top gh-proxy.com ghproxy.com; do curl -sS -o /dev/null -w "$m:%{http_code}\n" --connect-timeout 8 "https://$m"; done
   # ② 用通的镜像拉（例：ghfast.top）
   git pull https://ghfast.top/https://github.com/jxu520129-pixel/Quantitative-trading-model.git main
   ```
   注意：**本仓库是私有仓库**，镜像无法匿名拉取（会报 `Repository not found`）。需要先在 GitHub 网页把仓库临时设为 Public → 镜像拉取 → 拉完改回 Private。镜像测试结果备忘（2026-09-04）：`ghfast.top`、`gh-proxy.com` 通；`ghproxy.com` 被 reset、`mirror.ghproxy.com` 超时。
8. **策略勾选状态不进 git**：因子实验室策略的「启用」状态存在各自数据库（本地 Docker volume / 云端 volume 独立维护），改勾选不会随 push/pull 同步，需在云端单独改。`signal_strategies.enabled` 字段同时控制「盘中买点扫描」和「模拟盘 lab_signal」，可用 SQL 直接改：`docker exec ashare-quant-scheduler-1 python -c "import sqlite3; c=sqlite3.connect('/app/data/ashare_quant.db'); c.execute(\"UPDATE signal_strategies SET enabled=1 WHERE name='主升浪·启动突破'\"); c.commit()"`。
